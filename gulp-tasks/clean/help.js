/**
 * Hilfe über clean
 */
module.exports = function() {
    console.log("clean:build: delets all builded files (wwwroot/build folder)");
    console.log("clean:style  : delets all created style files (wwwroot/style folder)");
    console.log("clean:all  : delets all created files (wwwroot folder)");
}